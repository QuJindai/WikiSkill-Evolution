# V0.3 Task 4 compatibility diagnostic

pytest_exit_code=1

```text
============================= test session starts ==============================
platform linux -- Python 3.12.14, pytest-9.1.1, pluggy-1.6.0 -- /opt/hostedtoolcache/Python/3.12.14/x64/bin/python
cachedir: .pytest_cache
rootdir: /home/runner/work/WikiSkill-Evolution/WikiSkill-Evolution
configfile: pyproject.toml
collecting ... collected 31 items

tests/test_harness.py::test_evolve_accept_path PASSED                    [  3%]
tests/test_harness.py::test_evolve_reject_rolls_back_skills_keeps_wiki PASSED [  6%]
tests/test_harness.py::test_evolve_no_action_skips_gate PASSED           [  9%]
tests/test_harness.py::test_evolve_early_stop_at_perfect PASSED          [ 12%]
tests/test_harness.py::test_evolve_no_early_stop_flag PASSED             [ 16%]
tests/test_harness.py::test_evolve_accepts_prompt_candidate PASSED       [ 19%]
tests/test_harness.py::test_evolve_rejects_prompt_and_restores_previous_overlay PASSED [ 22%]
tests/test_harness.py::test_evolve_accepts_harness_candidate PASSED      [ 25%]
tests/test_harness.py::test_invalid_candidate_is_audited_and_skips_gate PASSED [ 29%]
tests/test_harness.py::test_active_harness_policy_controls_role_budgets PASSED [ 32%]
tests/test_harness.py::test_bootstrap_and_dry_run PASSED                 [ 35%]
tests/test_harness.py::test_patch_profile_model_rewrites_config PASSED   [ 38%]
tests/test_harness.py::test_fresh_sandbox_prevents_stale_scores PASSED   [ 41%]
tests/test_harness.py::test_zero_tool_call_session_detected PASSED       [ 45%]
tests/test_harness.py::test_evolve_accepts_provider_kwarg PASSED         [ 48%]
tests/test_harness.py::test_wiki_gets_audit_trail_commits FAILED         [ 51%]
tests/test_assets.py::test_legacy_proposal_defaults_to_skill PASSED      [ 54%]
tests/test_assets.py::test_unknown_target_fails_closed PASSED            [ 58%]
tests/test_assets.py::test_skill_driver_keeps_legacy_active_dir PASSED   [ 61%]
tests/test_assets.py::test_prompt_create_changes_only_workspace_prompt PASSED [ 64%]
tests/test_assets.py::test_prompt_reject_rollback_restores_exact_bytes PASSED [ 67%]
tests/test_assets.py::test_prompt_safe_path_rejects_traversal_and_absolute_path PASSED [ 70%]
tests/test_assets.py::test_harness_policy_validation_and_roundtrip PASSED [ 74%]
tests/test_assets.py::test_harness_policy_rejects_unknown_bool_and_out_of_range PASSED [ 77%]
tests/test_assets.py::test_harness_reject_rollback_restores_exact_policy PASSED [ 80%]
tests/test_assets.py::test_core_target_is_known_but_mutation_is_unsupported FAILED [ 83%]
tests/test_v02_contract.py::test_proposer_prompt_exposes_all_v02_asset_targets PASSED [ 87%]
tests/test_v02_contract.py::test_cli_evolve_default_does_not_override_harness_policy PASSED [ 90%]
tests/test_v02_contract.py::test_cli_run_task_forwards_max_turns_once PASSED [ 93%]
tests/test_v02_safety.py::test_skill_create_prevalidates_purpose_md_before_mutation PASSED [ 96%]
tests/test_v02_safety.py::test_harness_rolls_back_if_apply_fails_after_prepare FAILED [100%]

=================================== FAILURES ===================================
______________________ test_wiki_gets_audit_trail_commits ______________________

tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_wiki_gets_audit_trail_com0')

    def test_wiki_gets_audit_trail_commits(tmp_path):
        ws = build_ws(tmp_path)
        val = val_ids(ws)
        runner = FakeRunner(
            val_pass={0: set(val[:2]), 1: set(val[:4])},
            proposals={1: _proposal_create()},
        )
        harness.evolve(ws, iters=1, runner=runner, verbose=False)
        log = subprocess.run(["git", "-C", os.path.join(ws, "wiki"), "log", "--oneline"],
                             capture_output=True, text=True).stdout
        assert "wiki skeleton" in log
        assert "maintainer updates" in log
>       assert "gate outcome" in log
E       AssertionError: assert 'gate outcome' in '9b22c07 iter-01: accepted target=skill\n0dcd577 iter-01: maintainer updates\n65b78f5 wiki skeleton\n'

tests/test_harness.py:370: AssertionError
____________ test_core_target_is_known_but_mutation_is_unsupported _____________

tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_core_target_is_known_but_0')

    def test_core_target_is_known_but_mutation_is_unsupported(tmp_path):
        d = assets.resolve_driver("core")
        assert d.target == "core"
>       with pytest.raises(ValueError, match="core adapter.*unsupported"):
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'core adapter.*unsupported'
E         Actual message: "unknown core proposal keys: ['manifest', 'name']"

tests/test_assets.py:150: AssertionError
_____________ test_harness_rolls_back_if_apply_fails_after_prepare _____________

tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_harness_rolls_back_if_app0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7fe189640d40>

    def test_harness_rolls_back_if_apply_fails_after_prepare(tmp_path, monkeypatch):
        ws = str(tmp_path / "ws")
        os.makedirs(ws, exist_ok=True)
        wiki.ensure(ws)
        gating.ensure_active_repo(ws)
        task = {
            "id": "t1",
            "split": "train",
            "title": "T",
            "prompt": "P",
            "sandbox": {"input.txt": "fixture"},
            "grader": {"type": "contains", "file": "out.txt", "needle": "ok"},
        }
        val = dict(task, id="v1", split="val")
        tasks_mod.save(ws, [task, val])
        gating.save_state(ws, {
            "domain": "ws",
            "baseline": 0.0,
            "r_best": 0.0,
            "next_iter": 1,
            "history": [],
        })
    
        monkeypatch.setattr(gating, "run_task", lambda ws, task, it, **kw: {**task, "score": 0.0})
        monkeypatch.setattr(harness, "maintain_step", lambda *a, **k: {})
        monkeypatch.setattr(
            harness,
            "propose_step",
            lambda *a, **k: ({
                "target": "prompt",
                "action": "create",
                "name": "inference",
                "content": "candidate",
            }, {}),
        )
    
        class ExplodingDriver:
            target = "prompt"
    
            def __init__(self):
                self.rolled_back = False
                self.partial = os.path.join(ws, "assets", "prompts", "active", "partial.txt")
    
            def validate(self, ws, proposal):
                return None
    
            def prepare(self, ws, iteration):
                os.makedirs(os.path.dirname(self.partial), exist_ok=True)
    
            def apply(self, ws, proposal):
                with open(self.partial, "w", encoding="utf-8") as f:
                    f.write("partial")
                raise ValueError("apply exploded")
    
            def diff(self, ws):
                return ""
    
            def accept(self, ws, iteration, score):
                raise AssertionError("invalid candidate must not be accepted")
    
            def rollback(self, ws):
                self.rolled_back = True
                if os.path.exists(self.partial):
                    os.remove(self.partial)
    
        exploding = ExplodingDriver()
        real_resolve = assets.resolve_driver
        monkeypatch.setattr(
            assets,
            "resolve_driver",
            lambda target: exploding if target == "prompt" else real_resolve(target),
        )
    
>       state = harness.evolve(ws, iters=1, verbose=False, runner=lambda *a, **k: {})
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/test_v02_safety.py:99: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

ws = '/tmp/pytest-of-runner/pytest-0/test_harness_rolls_back_if_app0/ws'
iters = 1, model = None, provider = None
runner = <function test_harness_rolls_back_if_apply_fails_after_prepare.<locals>.<lambda> at 0x7fe1893a6480>
dry_run = False, verbose = False, max_turns = None, no_early_stop = False

    def evolve(
        ws: str,
        iters: int = 3,
        model: str | None = None,
        provider: str | None = None,
        runner=agents.run_agent,
        dry_run: bool = False,
        verbose: bool = True,
        max_turns: int | None = None,
        no_early_stop: bool = False,
    ) -> dict:
        def log(message: str) -> None:
            if verbose:
                print(f"[wikiskill] {message}")
    
        splits = tasks_mod.splits(ws)
        train, val = splits["train"], splits["val"]
        state = gating.load_state(ws)
        if model and not dry_run:
            agents.patch_profile_model(ws, model, provider)
            log(f"profile default model → {model} (provider: {provider or 'unchanged'})")
            model = None
        wiki.ensure(ws)
    
        if state.get("baseline") is None:
            log(f"baseline validation: {len(val)} val tasks, S0=∅")
            gate0 = gating.run_gate(
                ws,
                val,
                0,
                model=model,
                runner=runner,
                dry_run=dry_run,
                overwrite=True,
                max_turns=max_turns,
            )
            state["baseline"] = gate0["mean"]
            state["r_best"] = gate0["mean"]
            wiki.append_log(
                ws,
                f"iter-00 baseline: R={gate0['mean']} (S0=∅, {len(val)} val tasks)",
            )
            gating.save_state(ws, state)
    
        for k in range(state["next_iter"], iters + 1):
            if state["r_best"] == 1.0 and not no_early_stop:
                log("R_best == 1.0 → early stop")
                break
    
            log(f"iter {k}/{iters}: train rollouts ({len(train)} tasks)")
            train_results = [
                gating.run_task(
                    ws,
                    task,
                    k,
                    model=model,
                    runner=runner,
                    dry_run=dry_run,
                    overwrite=True,
                    max_turns=max_turns,
                )
                for task in train
            ]
            train_mean = gating.mean_score(train_results)
    
            sampled = sample_traces(ws, k, train_results)
            log(f"iter {k}: wiki maintenance (sampled {len(sampled)} traces)")
            maintain_step(ws, k, sampled, runner=runner, dry_run=dry_run)
            wiki.commit(ws, f"iter-{k:02d}: maintainer updates")
    
            log(f"iter {k}: evolution proposal")
            proposal, _ = propose_step(
                ws, k, train_results, runner=runner, dry_run=dry_run
            )
            raw = proposal if proposal is not None else {"action": "no_action"}
            try:
                proposal = assets.normalize_proposal(raw)
            except ValueError as exc:
                target = raw.get("target", "skill") if isinstance(raw, dict) else "?"
                action = raw.get("action", "?") if isinstance(raw, dict) else "?"
                error = str(exc)
                state["history"].append(
                    _history_entry(
                        k,
                        train_mean,
                        target=target,
                        action=action,
                        status="invalid",
                        accepted=False,
                        proposal=f"invalid {target}",
                        error=error,
                    )
                )
                state["next_iter"] = k + 1
                gating.save_state(ws, state)
                wiki.append_skill_impact(
                    ws,
                    prompts.gate_outcome_entry(
                        ws,
                        k,
                        raw if isinstance(raw, dict) else {"raw": raw},
                        None,
                        False,
                        "",
                        state["r_best"],
                        status="invalid",
                        error=error,
                    ),
                )
                wiki.append_log(
                    ws,
                    f"iter-{k:02d}: train={train_mean} proposal=invalid "
                    f"target={target}: {error}",
                )
                wiki.commit(ws, f"iter-{k:02d}: invalid proposal")
                continue
    
            target = proposal["target"]
            action = proposal["action"]
            if action == "no_action":
                state["history"].append(
                    _history_entry(
                        k,
                        train_mean,
                        target=target,
                        action=action,
                        status="no_action",
                        accepted=False,
                        proposal="no_action",
                    )
                )
                wiki.append_skill_impact(
                    ws,
                    prompts.gate_outcome_entry(
                        ws,
                        k,
                        proposal,
                        None,
                        False,
                        "",
                        state["r_best"],
                        status="no_action",
                    ),
                )
                wiki.append_log(
                    ws, f"iter-{k:02d}: train={train_mean} proposal=no_action"
                )
                state["next_iter"] = k + 1
                gating.save_state(ws, state)
                wiki.commit(ws, f"iter-{k:02d}: no action")
                continue
    
            driver = None
            context = None
            prepared = False
            desc = f"{action} {target}"
            diff = ""
            pre_gates: list[dict] = []
            try:
                driver = assets.resolve_driver(target)
                driver.validate(ws, proposal)
>               context = driver.prepare(ws, k, proposal)
                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E               TypeError: test_harness_rolls_back_if_apply_fails_after_prepare.<locals>.ExplodingDriver.prepare() takes 3 positional arguments but 4 were given

wikiskill/harness.py:411: TypeError
=========================== short test summary info ============================
FAILED tests/test_harness.py::test_wiki_gets_audit_trail_commits - AssertionError: assert 'gate outcome' in '9b22c07 iter-01: accepted target=skill\n0dcd577 iter-01: maintainer updates\n65b78f5 wiki skeleton\n'
FAILED tests/test_assets.py::test_core_target_is_known_but_mutation_is_unsupported - AssertionError: Regex pattern did not match.
  Expected regex: 'core adapter.*unsupported'
  Actual message: "unknown core proposal keys: ['manifest', 'name']"
FAILED tests/test_v02_safety.py::test_harness_rolls_back_if_apply_fails_after_prepare - TypeError: test_harness_rolls_back_if_apply_fails_after_prepare.<locals>.ExplodingDriver.prepare() takes 3 positional arguments but 4 were given
========================= 3 failed, 28 passed in 3.72s =========================
```
