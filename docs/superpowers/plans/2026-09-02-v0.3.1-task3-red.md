# V0.3.1 Task 3 RED

Expected failure: BoundRuntimeSession/bind_sha/bind_active_accepted are absent.

```text
.....FFFFFFFF                                                            [100%]
=================================== FAILURES ===================================
_____ test_accepted_binding_uses_detached_accepted_sha_not_dirty_checkout ______

tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_accepted_binding_uses_det0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f9376745100>

    def test_accepted_binding_uses_detached_accepted_sha_not_dirty_checkout(tmp_path, monkeypatch):
        ws, repo, sha = _register_source(
            tmp_path, runtime_script=_runtime_script("ACCEPTED")
        )
        _activate(ws)
        (repo / "wikiskill_runtime.py").write_text(
            _runtime_script("DIRTY_CHECKOUT"), encoding="utf-8"
        )
        monkeypatch.setenv("WIKISKILL_TEST_SECRET", "do-not-forward")
    
>       bound = runtime_bindings.bind_active_accepted(ws)
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_active_accepted'

tests/test_runtime_bindings.py:195: AttributeError
_____________________ test_missing_entrypoint_fails_closed _____________________

tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_missing_entrypoint_fails_0')

    def test_missing_entrypoint_fails_closed(tmp_path):
        ws, _, _ = _register_source(tmp_path, runtime_script=None)
        _activate(ws)
        with pytest.raises(ValueError, match="entrypoint"):
>           runtime_bindings.bind_active_accepted(ws)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_active_accepted'

tests/test_runtime_bindings.py:231: AttributeError
_____ test_candidate_binding_requires_exact_head_and_does_not_own_worktree _____

tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_candidate_binding_require0')

    def test_candidate_binding_requires_exact_head_and_does_not_own_worktree(tmp_path):
        ws, _, base_sha = _register_source(
            tmp_path, runtime_script=_runtime_script("OLD")
        )
        _activate(ws)
        proposal = {
            "target": "core",
            "action": "patch",
            "source_id": "demo-core",
            "base_sha": base_sha,
            "edits": [
                {
                    "file": "wikiskill_runtime.py",
                    "op": "replace",
                    "target": "OLD",
                    "content": "NEW",
                }
            ],
        }
        candidate = core_adapter.begin_candidate(ws, 1, proposal)
        core_adapter.apply_candidate(candidate)
        sealed = core_adapter.seal_candidate(candidate, 1)
    
        with pytest.raises(runtime_bindings.RuntimeBindingError, match="HEAD|SHA"):
>           runtime_bindings.bind_sha(
            ^^^^^^^^^^^^^^^^^^^^^^^^^
                ws,
                "demo-core",
                base_sha,
                candidate_worktree=candidate.worktree,
            )
E           AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_sha'

tests/test_runtime_bindings.py:258: AttributeError
_ test_python_runtime_protocol_failures_are_operational[import json\nimport os\nimport sys\nimport time\n\nrequest = json.loads(sys.stdin.readline())\nif 0:\n    time.sleep(0)\nworkdir = request["workdir"]\nos.makedirs(workdir, exist_ok=True)\nsecret = os.environ.get("WIKISKILL_TEST_SECRET", "<missing>")\nwith open(os.path.join(workdir, "out.txt"), "w", encoding="utf-8") as handle:\n    handle.write('BOUND' + "|secret=" + secret)\nprint('not-json')\nraise SystemExit(0)\n-JSON] _

tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_python_runtime_protocol_f0')
script = 'import json\nimport os\nimport sys\nimport time\n\nrequest = json.loads(sys.stdin.readline())\nif 0:\n    time.sleep(...ing="utf-8") as handle:\n    handle.write(\'BOUND\' + "|secret=" + secret)\nprint(\'not-json\')\nraise SystemExit(0)\n'
error = 'JSON'

    @pytest.mark.parametrize(
        "script, error",
        [
            (_runtime_script(response="not-json"), "JSON"),
            (_runtime_script(response='{"status":"bad"}'), "status"),
            (_runtime_script(exit_code=7), "exit"),
        ],
    )
    def test_python_runtime_protocol_failures_are_operational(tmp_path, script, error):
        ws, _, _ = _register_source(tmp_path, runtime_script=script)
        _activate(ws)
>       bound = runtime_bindings.bind_active_accepted(ws)
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_active_accepted'

tests/test_runtime_bindings.py:291: AttributeError
_ test_python_runtime_protocol_failures_are_operational[import json\nimport os\nimport sys\nimport time\n\nrequest = json.loads(sys.stdin.readline())\nif 0:\n    time.sleep(0)\nworkdir = request["workdir"]\nos.makedirs(workdir, exist_ok=True)\nsecret = os.environ.get("WIKISKILL_TEST_SECRET", "<missing>")\nwith open(os.path.join(workdir, "out.txt"), "w", encoding="utf-8") as handle:\n    handle.write('BOUND' + "|secret=" + secret)\nprint('{"status":"bad"}')\nraise SystemExit(0)\n-status] _

tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_python_runtime_protocol_f1')
script = 'import json\nimport os\nimport sys\nimport time\n\nrequest = json.loads(sys.stdin.readline())\nif 0:\n    time.sleep(...-8") as handle:\n    handle.write(\'BOUND\' + "|secret=" + secret)\nprint(\'{"status":"bad"}\')\nraise SystemExit(0)\n'
error = 'status'

    @pytest.mark.parametrize(
        "script, error",
        [
            (_runtime_script(response="not-json"), "JSON"),
            (_runtime_script(response='{"status":"bad"}'), "status"),
            (_runtime_script(exit_code=7), "exit"),
        ],
    )
    def test_python_runtime_protocol_failures_are_operational(tmp_path, script, error):
        ws, _, _ = _register_source(tmp_path, runtime_script=script)
        _activate(ws)
>       bound = runtime_bindings.bind_active_accepted(ws)
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_active_accepted'

tests/test_runtime_bindings.py:291: AttributeError
_ test_python_runtime_protocol_failures_are_operational[import json\nimport os\nimport sys\nimport time\n\nrequest = json.loads(sys.stdin.readline())\nif 0:\n    time.sleep(0)\nworkdir = request["workdir"]\nos.makedirs(workdir, exist_ok=True)\nsecret = os.environ.get("WIKISKILL_TEST_SECRET", "<missing>")\nwith open(os.path.join(workdir, "out.txt"), "w", encoding="utf-8") as handle:\n    handle.write('BOUND' + "|secret=" + secret)\nprint('{"status":"ok"}')\nraise SystemExit(7)\n-exit] _

tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_python_runtime_protocol_f2')
script = 'import json\nimport os\nimport sys\nimport time\n\nrequest = json.loads(sys.stdin.readline())\nif 0:\n    time.sleep(...f-8") as handle:\n    handle.write(\'BOUND\' + "|secret=" + secret)\nprint(\'{"status":"ok"}\')\nraise SystemExit(7)\n'
error = 'exit'

    @pytest.mark.parametrize(
        "script, error",
        [
            (_runtime_script(response="not-json"), "JSON"),
            (_runtime_script(response='{"status":"bad"}'), "status"),
            (_runtime_script(exit_code=7), "exit"),
        ],
    )
    def test_python_runtime_protocol_failures_are_operational(tmp_path, script, error):
        ws, _, _ = _register_source(tmp_path, runtime_script=script)
        _activate(ws)
>       bound = runtime_bindings.bind_active_accepted(ws)
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_active_accepted'

tests/test_runtime_bindings.py:291: AttributeError
___________________ test_multiple_json_objects_are_rejected ____________________

tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_multiple_json_objects_are0')

    def test_multiple_json_objects_are_rejected(tmp_path):
        script = '''import json, os, sys\nrequest=json.loads(sys.stdin.readline())\nprint(json.dumps({"status":"ok"}))\nprint(json.dumps({"status":"ok"}))\n'''
        ws, _, _ = _register_source(tmp_path, runtime_script=script)
        _activate(ws)
>       bound = runtime_bindings.bind_active_accepted(ws)
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_active_accepted'

tests/test_runtime_bindings.py:311: AttributeError
_________ test_python_runtime_timeout_and_missing_sandbox_fail_closed __________

tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_python_runtime_timeout_an0')

    def test_python_runtime_timeout_and_missing_sandbox_fail_closed(tmp_path):
        profile_id = "registered:test-python-timeout"
        registry = {
            profile_id: runtime_bindings.RuntimeBindingProfile(
                profile_id=profile_id,
                protocol="wikiskill-runtime-v1",
                timeout_s=1,
                entrypoint="wikiskill_runtime.py",
            )
        }
        ws, _, _ = _register_source(
            tmp_path, runtime_script=_runtime_script("SLOW", sleep_s=2)
        )
        _activate(ws, profile_id=profile_id, registry=registry)
>       bound = runtime_bindings.bind_active_accepted(ws, registry=registry)
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_active_accepted'

tests/test_runtime_bindings.py:341: AttributeError
=========================== short test summary info ============================
FAILED tests/test_runtime_bindings.py::test_accepted_binding_uses_detached_accepted_sha_not_dirty_checkout - AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_active_accepted'
FAILED tests/test_runtime_bindings.py::test_missing_entrypoint_fails_closed - AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_active_accepted'
FAILED tests/test_runtime_bindings.py::test_candidate_binding_requires_exact_head_and_does_not_own_worktree - AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_sha'
FAILED tests/test_runtime_bindings.py::test_python_runtime_protocol_failures_are_operational[import json\nimport os\nimport sys\nimport time\n\nrequest = json.loads(sys.stdin.readline())\nif 0:\n    time.sleep(0)\nworkdir = request["workdir"]\nos.makedirs(workdir, exist_ok=True)\nsecret = os.environ.get("WIKISKILL_TEST_SECRET", "<missing>")\nwith open(os.path.join(workdir, "out.txt"), "w", encoding="utf-8") as handle:\n    handle.write('BOUND' + "|secret=" + secret)\nprint('not-json')\nraise SystemExit(0)\n-JSON] - AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_active_accepted'
FAILED tests/test_runtime_bindings.py::test_python_runtime_protocol_failures_are_operational[import json\nimport os\nimport sys\nimport time\n\nrequest = json.loads(sys.stdin.readline())\nif 0:\n    time.sleep(0)\nworkdir = request["workdir"]\nos.makedirs(workdir, exist_ok=True)\nsecret = os.environ.get("WIKISKILL_TEST_SECRET", "<missing>")\nwith open(os.path.join(workdir, "out.txt"), "w", encoding="utf-8") as handle:\n    handle.write('BOUND' + "|secret=" + secret)\nprint('{"status":"bad"}')\nraise SystemExit(0)\n-status] - AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_active_accepted'
FAILED tests/test_runtime_bindings.py::test_python_runtime_protocol_failures_are_operational[import json\nimport os\nimport sys\nimport time\n\nrequest = json.loads(sys.stdin.readline())\nif 0:\n    time.sleep(0)\nworkdir = request["workdir"]\nos.makedirs(workdir, exist_ok=True)\nsecret = os.environ.get("WIKISKILL_TEST_SECRET", "<missing>")\nwith open(os.path.join(workdir, "out.txt"), "w", encoding="utf-8") as handle:\n    handle.write('BOUND' + "|secret=" + secret)\nprint('{"status":"ok"}')\nraise SystemExit(7)\n-exit] - AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_active_accepted'
FAILED tests/test_runtime_bindings.py::test_multiple_json_objects_are_rejected - AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_active_accepted'
FAILED tests/test_runtime_bindings.py::test_python_runtime_timeout_and_missing_sandbox_fail_closed - AttributeError: module 'wikiskill.runtime_bindings' has no attribute 'bind_active_accepted'
8 failed, 5 passed in 0.41s
```
