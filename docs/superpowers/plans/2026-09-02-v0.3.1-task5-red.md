# V0.3.1 Task 5 RED

Expected failure: harness has no accepted-runtime scoring, candidate binding transaction, or recovery semantics yet.

```text
============================= test session starts ==============================
platform linux -- Python 3.12.14, pytest-9.1.1, pluggy-1.6.0 -- /opt/hostedtoolcache/Python/3.12.14/x64/bin/python
cachedir: .pytest_cache
rootdir: /home/runner/work/WikiSkill-Evolution/WikiSkill-Evolution
configfile: pyproject.toml
collecting ... collected 8 items

tests/test_v031_runtime_integration.py::test_active_runtime_owns_baseline_and_records_identity FAILED [ 12%]
tests/test_v031_runtime_integration.py::test_no_active_runtime_preserves_legacy_runner PASSED [ 25%]
tests/test_v031_runtime_integration.py::test_non_core_candidate_held_out_uses_same_accepted_runtime FAILED [ 37%]
tests/test_v031_runtime_integration.py::test_core_candidate_real_runtime_causes_acceptance FAILED [ 50%]
tests/test_v031_runtime_integration.py::test_core_non_improvement_executes_candidate_then_rejects FAILED [ 62%]
tests/test_v031_runtime_integration.py::test_next_iteration_rebinds_newly_accepted_runtime FAILED [ 75%]
tests/test_v031_runtime_integration.py::test_state_save_failure_after_source_transition_compensates_to_A FAILED [ 87%]
tests/test_v031_runtime_integration.py::test_compensation_failure_enters_recovery_required_and_blocks_scoring FAILED [100%]

=================================== FAILURES ===================================
____________ test_active_runtime_owns_baseline_and_records_identity ____________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7ff5cbb3faa0>
tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_active_runtime_owns_basel0')

    def test_active_runtime_owns_baseline_and_records_identity(monkeypatch, tmp_path):
        ws, _, sha = _setup_workspace(tmp_path, active=True, answer="wrong")
        framework = FrameworkRunner(task_answer="framework")
    
        state = harness.evolve(ws, iters=0, runner=framework, verbose=False)
    
        assert state["baseline"] == 0.0
        assert state["r_best"] == 0.0
>       assert state["runtime_identity"] == {
               ^^^^^^^^^^^^^^^^^^^^^^^^^
            "source_id": "demo-core",
            "binding_profile": "registered:python-json-runner-v1",
            "accepted_sha": sha,
            "fingerprint": runtime_bindings.active_runtime_config(ws)["fingerprint"],
        }
E       KeyError: 'runtime_identity'

tests/test_v031_runtime_integration.py:200: KeyError
_________ test_non_core_candidate_held_out_uses_same_accepted_runtime __________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7ff5cbb9fd40>
tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_non_core_candidate_held_o0')

    def test_non_core_candidate_held_out_uses_same_accepted_runtime(monkeypatch, tmp_path):
        ws, _, sha = _setup_workspace(tmp_path, active=True, answer="wrong")
        framework = FrameworkRunner(task_answer="ok")
        _stub_roles(monkeypatch, framework, lambda k: _prompt_proposal())
    
        state = harness.evolve(ws, iters=1, runner=framework, verbose=False)
    
>       assert _runtime_meta(ws, 1, "train", "train-one")["source_sha"] == sha
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/test_v031_runtime_integration.py:231: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_v031_runtime_integration.py:185: in _runtime_meta
    return traces.load_trace(ws, iteration, split, task_id).get("runtime")
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

ws = '/tmp/pytest-of-runner/pytest-0/test_non_core_candidate_held_o0/ws', it = 1
split = 'train', tid = 'train-one'

    def load_trace(ws: str, it: int, split: str, tid: str) -> dict:
>       with open(meta_path(ws, it, split, tid), encoding="utf-8") as f:
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       FileNotFoundError: [Errno 2] No such file or directory: '/tmp/pytest-of-runner/pytest-0/test_non_core_candidate_held_o0/ws/raw/traces/iter-01/train/train-one.meta.json'

wikiskill/traces.py:45: FileNotFoundError
______________ test_core_candidate_real_runtime_causes_acceptance ______________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7ff5cbba3ec0>
tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_core_candidate_real_runti0')

    def test_core_candidate_real_runtime_causes_acceptance(monkeypatch, tmp_path):
        ws, repo, sha_a = _setup_workspace(tmp_path, active=True, answer="wrong")
        framework = FrameworkRunner(task_answer="framework")
        _stub_roles(monkeypatch, framework, lambda k: _core_proposal(sha_a, "ok"))
    
        state = harness.evolve(ws, iters=1, runner=framework, verbose=False)
    
        sha_b = sources.accepted_sha(ws, "demo-core")
>       assert sha_b != sha_a
E       AssertionError: assert '1df19fd1cad073149c2d6ca1088066c4c51d17bd' != '1df19fd1cad073149c2d6ca1088066c4c51d17bd'

tests/test_v031_runtime_integration.py:246: AssertionError
__________ test_core_non_improvement_executes_candidate_then_rejects ___________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7ff5cbb9f560>
tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_core_non_improvement_exec0')

    def test_core_non_improvement_executes_candidate_then_rejects(monkeypatch, tmp_path):
        ws, repo, sha_a = _setup_workspace(tmp_path, active=True, answer="wrong")
        framework = FrameworkRunner(task_answer="ok")
        _stub_roles(monkeypatch, framework, lambda k: _core_proposal(sha_a, "still-wrong"))
    
        state = harness.evolve(ws, iters=1, runner=framework, verbose=False)
    
>       item = state["history"][-1]
               ^^^^^^^^^^^^^^^^^^^^
E       IndexError: list index out of range

tests/test_v031_runtime_integration.py:267: IndexError
______________ test_next_iteration_rebinds_newly_accepted_runtime ______________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7ff5cbba3cb0>
tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_next_iteration_rebinds_ne0')

    def test_next_iteration_rebinds_newly_accepted_runtime(monkeypatch, tmp_path):
        ws, _, sha_a = _setup_workspace(tmp_path, active=True, answer="wrong")
        framework = FrameworkRunner(task_answer="framework")
    
        def proposal(k):
            if k == 1:
                return _core_proposal(sha_a, "ok")
            return {"action": "no_action"}
    
        _stub_roles(monkeypatch, framework, proposal)
        state = harness.evolve(
            ws, iters=2, runner=framework, verbose=False, no_early_stop=True
        )
    
>       sha_b = state["runtime_identity"]["accepted_sha"]
                ^^^^^^^^^^^^^^^^^^^^^^^^^
E       KeyError: 'runtime_identity'

tests/test_v031_runtime_integration.py:292: KeyError
_______ test_state_save_failure_after_source_transition_compensates_to_A _______

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7ff5cb9e3e90>
tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_state_save_failure_after_0')

    def test_state_save_failure_after_source_transition_compensates_to_A(monkeypatch, tmp_path):
        ws, repo, sha_a = _setup_workspace(tmp_path, active=True, answer="wrong")
        framework = FrameworkRunner(task_answer="framework")
        harness.evolve(ws, iters=0, runner=framework, verbose=False)
        _stub_roles(monkeypatch, framework, lambda k: _core_proposal(sha_a, "ok"))
    
        real_save = gating.save_state
        failed = {"done": False}
    
        def fail_once(got_ws, state):
            identity = state.get("runtime_identity") or {}
            if (
                not failed["done"]
                and state.get("r_best") == 1.0
                and identity.get("accepted_sha") not in (None, sha_a)
            ):
                failed["done"] = True
                raise OSError("simulated final scoring-state failure")
            return real_save(got_ws, state)
    
        monkeypatch.setattr(gating, "save_state", fail_once)
        state = harness.evolve(ws, iters=1, runner=framework, verbose=False)
    
        persisted = gating.load_state(ws)
>       assert failed["done"] is True
E       assert False is True

tests/test_v031_runtime_integration.py:322: AssertionError
____ test_compensation_failure_enters_recovery_required_and_blocks_scoring _____

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7ff5cbba0e00>
tmp_path = PosixPath('/tmp/pytest-of-runner/pytest-0/test_compensation_failure_ente0')

    def test_compensation_failure_enters_recovery_required_and_blocks_scoring(monkeypatch, tmp_path):
        ws, repo, sha_a = _setup_workspace(tmp_path, active=True, answer="wrong")
        framework = FrameworkRunner(task_answer="framework")
        harness.evolve(ws, iters=0, runner=framework, verbose=False)
        _stub_roles(monkeypatch, framework, lambda k: _core_proposal(sha_a, "ok"))
    
        real_save = gating.save_state
        failed = {"done": False}
    
        def fail_once(got_ws, state):
            identity = state.get("runtime_identity") or {}
            if (
                not failed["done"]
                and state.get("r_best") == 1.0
                and identity.get("accepted_sha") not in (None, sha_a)
            ):
                failed["done"] = True
                raise OSError("simulated final scoring-state failure")
            return real_save(got_ws, state)
    
        monkeypatch.setattr(gating, "save_state", fail_once)
    
        def no_compensation(*args, **kwargs):
            raise core_adapter.CoreOperationalError("simulated compensation failure")
    
        monkeypatch.setattr(core_adapter, "compensate_accepted_candidate", no_compensation)
        state = harness.evolve(ws, iters=1, runner=framework, verbose=False)
    
        sha_b = sources.accepted_sha(ws, "demo-core")
        persisted = gating.load_state(ws)
>       assert sha_b != sha_a
E       AssertionError: assert 'baaef1a494e185ac7e38bbb1f1f5bdf73cc351a7' != 'baaef1a494e185ac7e38bbb1f1f5bdf73cc351a7'

tests/test_v031_runtime_integration.py:360: AssertionError
=========================== short test summary info ============================
FAILED tests/test_v031_runtime_integration.py::test_active_runtime_owns_baseline_and_records_identity - KeyError: 'runtime_identity'
FAILED tests/test_v031_runtime_integration.py::test_non_core_candidate_held_out_uses_same_accepted_runtime - FileNotFoundError: [Errno 2] No such file or directory: '/tmp/pytest-of-runner/pytest-0/test_non_core_candidate_held_o0/ws/raw/traces/iter-01/train/train-one.meta.json'
FAILED tests/test_v031_runtime_integration.py::test_core_candidate_real_runtime_causes_acceptance - AssertionError: assert '1df19fd1cad073149c2d6ca1088066c4c51d17bd' != '1df19fd1cad073149c2d6ca1088066c4c51d17bd'
FAILED tests/test_v031_runtime_integration.py::test_core_non_improvement_executes_candidate_then_rejects - IndexError: list index out of range
FAILED tests/test_v031_runtime_integration.py::test_next_iteration_rebinds_newly_accepted_runtime - KeyError: 'runtime_identity'
FAILED tests/test_v031_runtime_integration.py::test_state_save_failure_after_source_transition_compensates_to_A - assert False is True
FAILED tests/test_v031_runtime_integration.py::test_compensation_failure_enters_recovery_required_and_blocks_scoring - AssertionError: assert 'baaef1a494e185ac7e38bbb1f1f5bdf73cc351a7' != 'baaef1a494e185ac7e38bbb1f1f5bdf73cc351a7'
========================= 7 failed, 1 passed in 1.97s ==========================
```
